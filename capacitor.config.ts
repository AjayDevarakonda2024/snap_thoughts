import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'snap.thoughts.app',
  appName: 'Snap Thoughts',
  webDir: 'out',  // Pointing to the 'out' directory where static assets are built
};

export default config;
